# Mô hình 3 — Transformer-based
run_name: phobert_base_v2
family: transformer
model_id: vinai/phobert-base-v2
revision: 86cd7fd4c148980922ac11a2cf5e257f2ba639e1   # commit hash chốt cho báo cáo
license: agpl-3.0
word_segment: true        # PhoBERT BẮT BUỘC tách từ trước
# Tên cột được dò tự động bằng data._resolve_columns() -> không khai báo ở đây.

seed: 42
max_length: 128
batch_size: 32
grad_accum: 1
epochs: 8
learning_rate: 2.0e-5
warmup_ratio: 0.06
weight_decay: 0.01
fp16: true
gradient_checkpointing: false
